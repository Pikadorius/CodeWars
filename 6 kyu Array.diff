function arrayDiff(a, b) {
  let arr=[];
  for(let i=0;i<a.length; i++) {
    if(b.includes(a[i])) continue;
    else arr.push(a[i]);
  }
  return arr;
}

___________________________easiest way

function array_diff(a, b) {
  return a.filter(item => !b.includes(item));
}
