function capitalize(s,arr){
  let res=s.split('')
  for (let i=0; i<arr.length; i++) {
    if (res[arr[i]]===undefined) continue
    res[arr[i]]=res[arr[i]].toUpperCase()
  }
  return res.join("")
};
