function divisibleBy(numbers, divisor){
  let divisibleNumbers = [];
  for (let i=0; i<numbers.length; i++) {
    if (numbers[i]%divisor===0) divisibleNumbers.push(numbers[i]);
    else continue;
  }
  return divisibleNumbers;
}
