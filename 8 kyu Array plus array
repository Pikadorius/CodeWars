function arrayPlusArray(arr1, arr2) {
  let count1=0;
  for (i=0; i<arr1.length; i++) {
    count1 += arr1[i];
  }
  let count2=0;
   for (i=0; i<arr2.length; i++) {
    count2 += arr2[i];
    }
  return count1 + count2;
}
