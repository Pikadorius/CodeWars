const rps = (p1, p2) => {
  let game={
    scissors: "paper",
    paper: "rock",
    rock: "scissors"
  }
  
  if(game[p1]===game[p2]) return "Draw!";
    else if (game[p1].includes(p2)) return "Player 1 won!";
    else return "Player 2 won!"
  
};
