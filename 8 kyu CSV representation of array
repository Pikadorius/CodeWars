function toCsvText(array) {
   return array.join('\n');
}
_______________________________

function toCsvText(array) {
   // good luck
  let message='';
  for(let i of array) {
    message+=i +"\n";
  }
  return message.slice(0,-1);
}
